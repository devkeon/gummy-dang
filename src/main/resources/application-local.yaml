spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gummy?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1410
  jpa:
    hibernate:
      ddl-auto: create
      defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  sql:
    init:
      mode: always
